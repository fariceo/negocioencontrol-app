<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/negocioencontrol_ic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Probando"
        tools:targetApi="31">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".bodega.BodegaActivity"
            android:exported="false"
            android:label="@string/title_activity_bodega"
            android:theme="@style/Theme.Probando" />
        <activity
            android:name=".gastos.GastosActivity"
            android:exported="false"
            android:label="@string/title_activity_gastos"
            android:theme="@style/Theme.Probando" /> <!-- 👇 AGREGA ESTO -->
        <activity
            android:name=".login.RegistroActivity"
            android:exported="false"
            android:theme="@style/Theme.Probando" />
        <activity
            android:name=".subir_productos.SubirProductosActivity"
            android:exported="false"
            android:theme="@style/Theme.Probando" />
        <activity
            android:name=".scanner.CustomScannerActivity"
            android:exported="false" />
        <activity
            android:name=".login.LoginScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Probando">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Probando" />
        <activity
            android:name=".scanner.ScannerActivity"
            android:exported="false" />
        <activity
            android:name=".ventas.VentasActivity"
            android:exported="false"
            android:theme="@style/Theme.Probando" />

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>